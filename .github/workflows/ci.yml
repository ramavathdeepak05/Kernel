name: CI

on:
  push:
    branches: [main, "feat/**"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install (runtime + test + dev extras)
        # Single source of truth: New/quaicu-kernel/pyproject.toml (no hand-maintained dep list).
        run: |
          pip install --upgrade pip
          cd New/quaicu-kernel
          pip install -e ".[test,dev]"

      - name: Lint (ruff)
        # Non-blocking for now: the tree carries ~60 pre-existing lint findings (mostly unused
        # imports in tests) from before lint ran in CI. This step surfaces them as annotations
        # without failing the build; flip to blocking once that backlog is cleared in a dedicated
        # cleanup. New code is expected to be ruff-clean.
        continue-on-error: true
        run: |
          cd New/quaicu-kernel
          ruff check .

      - name: Run test suite
        # The Postgres + OpenBao integration tests self-skip without live services (10 skips).
        run: |
          cd New/quaicu-kernel
          python -m pytest --tb=short -q
