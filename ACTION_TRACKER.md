# QUAICU Kernel — Deployment-Readiness Action Tracker

> **Reset 2026-06-28.** The prior wave breakdown (W0–W8) was cleared from this file and replaced by the
> Deployment-Readiness Program below. Historical wave detail + the dated Progress Log remain in git
> history and in `New/quaicu-kernel/docs/adr/BUILD_JOURNAL.md` (Log). This file is now the granular,
> task-level tracker for taking the kernel to **production deployment**.

**How to use:** work top-to-bottom within a phase; phases are **dependency-ordered**, not calendar-bound
(decision: *correctness over deadline*). Tick the box, set **Status**, append a dated note to the
Progress Log at the bottom. **Type** = `code` (we build) / `infra` (cloud/IaC/ops) / `human`
(legal/audit/process — calendar time, we only sequence + track).

**Status legend:** `TODO` · `IN-PROGRESS` · `BLOCKED` · `DONE` · `DEFERRED`.

---

## Program scope — decisions of record (2026-06-28)
- **Shapes:** ship **both** dedicated single-tenant **and** shared multi-tenant SaaS.
- **Cloud:** **GCP**. **Readiness bar:** all three — design-partner pilot + regulated-enterprise GA + cloud-marketplace listing.
- **Proof layer:** build the **trust anchor + external witness now**; the third-party crypto review is **deferred but scheduled/tracked** (T-1).
- **HITL channels:** **Microsoft Teams + Email** (reuse the existing Resend sender). Slack/Jira out of scope.
- **Integration surfaces:** **harden the Python SDK**, build the **MCP governance server**, extend the **gateway to tool-calls**. Non-Python SDKs out of scope.
- **Policy authoring:** **QUAICU-authored CEL packs** (services-led). Rule-builder UI / multi-step SOP authoring out of scope.
- **Ops model:** **mixed** — QUAICU operates the shared SaaS; dedicated instances are **customer-operated** (their GCP project, their KMS) with our IaC + runbooks + support.
- **HA/DR:** build the capability; commit specific SLA/RTO/RPO **per contract**.
- **Residency:** **per-customer configurable** GCP region (deploy-time parameter + residency matrix).
- **Identity:** keep generic OIDC; add **SAML 2.0** + **Entra/Azure AD** integration & certification.
- **Billing:** activate **live Razorpay** (India self-serve) + **invoice/contract** (enterprise). GCP Marketplace *metered* billing deferred → listing-readiness only.
- **Member auth:** build **in-browser member login** so a compliance member does maker/checker in the console.
- **Definition of done:** **all three** — a live pilot running end-to-end **and** an external security review passed **and** supply-chain + load + DR-restore green.

## Verified baseline corrections (don't trust the stale .md notes — confirmed in code 2026-06-28)
- `pyproject.toml` **exists** → packaging is partly done; the real gap is **no lockfile**.
- `EntitlementRepository` (`core/entitlements/repository.py`) + `PostgresEntitlementRepository`
  (`adapters/entitlements/postgres.py`) **exist** → durable entitlements are built; verify they are wired, not rebuild.
- `build_saas_app` (`delivery/sdk/saas_app.py:76`) + `TieredKernelProvider` (`delivery/sdk/provider.py:29`)
  **exist** → the shared-plane entrypoint exists; harden, don't write from scratch.
- Identity adapters present: `adapters/identity/{jwt_adapter,oidc}.py` only → **SAML is genuinely missing**.
- Billing adapters present: `adapters/billing/{razorpay,stripe,marketplace,razorpay_signup}.py` → need live keys + the absent `RAZORPAY_WEBHOOK_SECRET`, not new code.

---

## Phase D0 — Ground truth + supply-chain foundations (unblocks everything)

| ID | Task | Type | Status |
|----|------|------|--------|
| ☑ D0-1 | **State-of-durability audit.** Trace every component on the operated plane to confirm it runs on its Postgres adapter (action repo, entitlements, ledger, policy store, HITL store) and that RLS isolation (migration 004) is active end-to-end. **DoD:** a written, code-cited gap list; any remaining in-memory hot-path on the operated plane is identified with the file. → **`New/quaicu-kernel/docs/operations/DURABILITY_AUDIT.md`** | code | DONE |
| ☑ D0-2 | **Dependency lockfile.** Add a lockfile pinning the deps declared in `pyproject.toml` + `delivery/docker/Dockerfile`. **DoD:** reproducible `pip install` from the lock; image builds from the lock; lock checked in. Prereq for D0-3 SBOM. → base `requirements.lock` already existed (hash-pinned); **closed the floating `pip install google-cloud-kms`** with a hash-pinned `requirements-gcp.lock` (consistent superset of base + `gcp` extra) installed via `--require-hashes`. | code | DONE |
| ☑ D0-3 | **Supply-chain CI → blocking.** Extend `cloudbuild.yaml`: lint + unit tests (blocking) → build → **CycloneDX SBOM** artifact → **cosign** image signing → **Trivy + pip-audit as gates** (graduate from report-mode). **DoD:** a failing scan/lint/test fails the build; every pushed image is signed; SBOM emitted per build. → pip-audit + Trivy (HIGH,CRITICAL, `--ignore-unfixed`) now **blocking**; SBOM emitted every build; **cosign sign + SBOM-attest via Cloud KMS** (`--tlog-upload=false`, zero-egress-safe). **Operator one-time step:** provision the KMS `cosign-signer` key + grant the Cloud Build SA `signerVerifier`, set `_COSIGN_KMS_KEY` (see VULN_MANAGEMENT.md §3). | infra | DONE (config) · needs KMS provisioning |
| ☑ D0-4 | **Listing/legal artifacts.** Add `LICENSE`; confirm `README` + `SECURITY.md` are listing-grade. **DoD:** LICENSE present; README states what it is + how to deploy; SECURITY.md has a disclosure path. → root artifacts were already good; the **package** (`New/quaicu-kernel/`) had no LICENSE/README + no `pyproject` license metadata. Added package `LICENSE` + listing-grade `README.md`; wired `pyproject` (PEP 639 `license` + `license-files` + `readme` + classifiers + urls); confirmed `SECURITY.md` disclosure path + de-staled its supply-chain bullet. **Verified via `uv build`:** wheel METADATA carries `License-Expression` + bundled LICENSE + markdown README. Legal placeholders remain counsel's job (T-4). | code | DONE |
| ☑ D0-5 | **Durable hot-path closure → reframed: shared durable plane + upgrade = feature unlock** (owner decision). Replaced the two-kernel plane with **one durable kernel** (`kernel.shared.toml`) serving STARTER+BUSINESS on Postgres+KMS; tier is a pure feature gate (entitlements). No in-memory hot-path → `--workers > 1` safe (resolves D0-1 Gap 1); a STARTER→BUSINESS upgrade moves **no data**. Added explicit fail-closed `max_policies` gating (5b) + raised caps (STARTER 200 / BUSINESS 1000). ADR-0013. **DoD:** unit suite green (1041); integration proofs **green live** against GCP Cloud SQL (`tests/conformance/storage/test_shared_plane.py` — 3 passed). | code | DONE |

## Phase D1 — Make the human-approval loop actually work (demo-breaking)

*Files: `core/lifecycle/engine.py` (default `max_poll_attempts=1`, `defer_gate`, `resume_after_approval`),
`delivery/sdk/kernel.py` (`guard`/`wrap`/`generate`, `resume_approved`), `delivery/api/routes/approvals.py`,
`core/ports/hitl.py`, `core/hitl/model.py`, `core/hitl/store.py`, `adapters/hitl/` (+ new `email.py`, `teams.py`).*

| ID | Task | Type | Status |
|----|------|------|--------|
| ☑ D1-1 | **Async-by-default approval on the SDK path.** Route `guard`/`wrap`/`generate` through the durable `defer_gate` + `resume_after_approval` flow that `/v1/actions/propose` already uses, so a `require_approval` action **suspends durably** instead of polling-once → `TIMED_OUT` → DENY. Keep fail-closed only on real infra failure. **DoD:** an SDK-decorated `require_approval` action returns PENDING and resumes+executes+seals on approval; no false DENY; test proves it. → `guard`/`wrap`/`generate`/`governed*` now run `defer_gate=True` and raise the new `LifecyclePendingApprovalError` (carries action_id + handle_id) on suspend; approve→resume+seal→COMPLETED, reject→DENIED, infra failure→HALT. `tests/unit/sdk/test_async_approval.py` (5 tests). **Note:** for `guard`/`wrap` the resume records the action payload + seals; it does not re-invoke the live closure (that's D1-4/propose). | code | DONE |
| ☑ D1-2 | **Email HITL adapter.** `EmailHITLAdapter` implementing `HITLPort`, reusing the existing Resend sender; sends an approval request with signed, expiring approve/reject links. **DoD:** approval round-trips via email link; link signature verified + single-use; config-selectable. → `EmailHITLAdapter(InProcessHITLPort)` (adapters/hitl/email.py) reuses the store+resume flow and emails signed links; new pure-stdlib `core/hitl/links.py` `ApprovalLinkSigner` (HMAC, expiring); `GET/POST /v1/approvals/link/{token}` (GET confirm page, POST commits — prefetch-safe, single-use via store); config-selectable `hitl = "email"` (`_build_email_hitl`, Resend-or-log-only, `QUAICU_APPROVAL_LINK_SECRET`). 9 tests. | code | DONE |
| ☑ D1-3 | **Microsoft Teams HITL adapter.** `MicrosoftTeamsHITLAdapter` (Adaptive Card with approve/reject actions + signed callback). **DoD:** card posts to a channel/webhook; approve/reject resolves the approval; tenant-isolated; config-selectable. → `MicrosoftTeamsHITLAdapter(InProcessHITLPort)` (adapters/hitl/teams.py) posts an Adaptive Card with two `Action.OpenUrl` buttons to a Teams incoming webhook; buttons reuse the D1-2 signed confirm-page flow (`ApprovalLinkSigner` + `/v1/approvals/link`), so decide/single-use/tenant-isolation are shared. Fail-closed on non-2xx / transport error. Config-selectable `hitl = "teams"` (`_build_teams_hitl`). Injectable `http_post` for tests. 4 tests. | code | DONE |
| ☑ D1-4 | **Durable, at-least-once delivery.** A delivery worker over the Postgres approval store (retry + dedupe); enrich `ApprovalRecord` with notification target/contact + a signed resume link. **DoD:** delivery survives a restart; no duplicate executions; record carries routing metadata. → **Reframed by owner** (fail-closed routing + pending-until-decided): **no worker**. Enriched `ApprovalRecord` with `notify_channel`/`notify_target`/`resume_link` (migration 014); email/Teams reordered to **send-then-persist** (a PENDING record ⇒ delivered notification; send failure → HALT, no orphan); durable adapters default to **no expiry** (pending until decided). Records + routing survive restart; idempotent resume = no duplicate executions. Unit suite **1061 passed**; migration 014 applied + routing-metadata round-trip **green live** on GCP Cloud SQL (4 integration tests). | code | DONE |
| ☑ D1-5 | **In-browser member maker/checker.** Member login (password + IdP) so a `COMPLIANCE` member approves in the console, not only via API key; keep the self-approval guard. **DoD:** a member logs in and approves in-browser; self-approval blocked; sealed approver identity correct. → Added member credentials (`Member.password_hash`, migration 015) + engine `mint_member_set_password_token`/`set_member_password`/`authenticate_member`/`mint_member_session` (session `sub=member_id`, `roles=(compliance,)`, `APPROVAL_DECIDE` scope). `/v1/auth/login` now account-or-member; new `/v1/auth/set-password`; invite emails a signed set-password link. Console `SetPassword` page + route (login form + Approvals page already existed). IdP = existing OIDC path (SAML is D5-1). **Verified:** full round-trip (member login→approve, sealed as member) + SoD (own-action→409); 1067 unit passed, console typecheck+build green. | code | DONE |

## Phase D2 — Interception surfaces (cooperative → harder to bypass)

| ID | Task | Type | Status |
|----|------|------|--------|
| ☑ D2-1 | **MCP governance server** (`delivery/mcp/`). A fail-closed MCP server that intercepts an agent's **tool calls**, maps each to an action (type + payload) → policy → HITL → seal, in one integration, no client-code change. **DoD:** an MCP client's tool calls are governed end-to-end (allow/deny/approval), each sealed; deny blocks the tool call; documented quickstart. → New `delivery/mcp/`: MCP-free core `govern_tool_call`/`ToolOutcome` (wraps `kernel.wrap`); `GovernedMCPServer` (registered tools) + `GovernedMCPProxy` (front a downstream) on the official `mcp` SDK (`[mcp]` extra, lazy import); `quaicu-kernel-mcp` proxy entrypoint; README quickstart. allow→run+seal, deny/halt→blocked, require_approval→PENDING+handle. **Surfaced + fixed a D1-1 bug:** `kernel.wrap` had missed the defer-gate change (still false-DENYed require_approval) — fixed + regression test. 12 tests. | code | DONE |
| ☑ D2-2 | **Gateway tool-call governance.** Extend `delivery/api/routes/ai_gateway.py` + `delivery/api/ai_providers.py` beyond chat-completions to **tool/function calls** (and embeddings) across providers; govern the tool call as the action. **DoD:** a provider tool-call flows through policy + masking + seal; tests per provider shim. → In a passthrough gateway the model's tool call appears in the **response**; shims now surface each provider's tool call in OpenAI shape (Anthropic `tool_use`⇄`tool_calls` req+resp+stream; Bedrock `toolUse` incl. `toolConfig`/`toolResult`; OpenAI/Vertex passthrough), and the route seals each as an **`ai.tool_call`** action (non-PII summary payload) via the existing `kernel.check` seal path — **any deny fails the whole response** (non-stream 403; stream buffers tool_call deltas, seals at end, emits only if allowed, else SSE error event). New governed **`POST /v1/ai/embeddings`** (`ai.embed`, masked input, budget; Anthropic/Bedrock → 501). Tests: `test_ai_tool_governance.py` (6) + `test_ai_embeddings.py` (6) + shim translations in `test_ai_providers.py`. | code | DONE |
| ☑ D2-3 | **Python SDK hardening.** Error semantics, async-approval ergonomics (pairs with D1-1), worked examples + docs. **DoD:** documented, typed, examples run green; clear exceptions on deny/halt/timeout. → **Error surface** now importable straight from `delivery.sdk` (`QUAICUError`, `Lifecycle{Denied,Halted,PendingApproval}Error`, + new **`SdkUsageError`**); the 7 ad-hoc `RuntimeError`/`TypeError` **misuse** sites (no actor in scope across guard/wrap/governed/governed_tool/proxy; no adapter on generate/decide_approval) now raise the typed `SdkUsageError(QUAICUError)` (defined in `delivery/sdk/errors.py`, not the frozen `core/errors.py`). Governance **outcomes** (deny/halt/pending) unchanged. **Ergonomics:** `kernel.approve()`/`kernel.reject()` thin wrappers over `decide_approval`, driven off `exc.detail["handle_id"]`. **Typed:** PEP 561 `py.typed` shipped for core/adapters/delivery (verified in wheel). **Runnable example** `examples/sdk-approval-demo/` (allow→deny→approve→SoD-block) + docs (SDK reference Exceptions/Async-approval sections; tutorial rewritten). Tests: `test_sdk_error_semantics.py` (8) + `test_approval_ergonomics.py` (3) + `test_sdk_approval_demo.py` (1, runs the example). | code | DONE |

## Phase D3 — Proof layer: trust anchor + external witness (anchor now, audit later)

*Files: `core/regmap/export.py` (`verify_ledger_proof_bundle`), `core/ledger/signer.py`,
`core/ledger/engine.py`, `adapters/ledger/{gcp_kms,aws_kms,openbao}.py`; new `core/ledger/anchor.py` + witness adapter.*

| ID | Task | Type | Status |
|----|------|------|--------|
| ☑ D3-1 | **Key attestation / pinning.** Stop trusting the public key embedded in the bundle. Establish an out-of-band key identity (published key registry / pinned per-tenant `key_id`); change `verify_ledger_proof_bundle` to verify against an **externally supplied/pinned** key, not the bundle's own. Document the trust model. **DoD:** a bundle signed by an unknown/forged key **fails** verification; a correct externally-pinned key passes; test covers both. → `verify_ledger_proof_bundle(bundle, *, trusted_keys={key_id: pem})` now pins authenticity to a caller-supplied key (fail-closed: no anchor/unknown key_id/sig-mismatch/embedded≠pinned → fail); integrity (inclusion proofs) stays self-contained. New `GET /v1/ledger/signing-key` publishes `{key_id, public_key_pem, algorithm}` to pin at onboarding; `/export/verify` pins to the tenant kernel's key. `trusted_keys_from_signer` helper; `public_key_pem` added to the `TreeSigner` protocol. Trust-model doc + 5 DoD tests. | code | DONE |
| ☑ D3-2 | **External witness / anchoring.** Periodically publish each tenant's STH to an independent anchor (**decide in this task:** RFC-3161 TSA vs a witness service vs a public append-only endpoint) + continuous consistency-proof checking across successive STHs; surface anchor proofs in the export bundle. **DoD:** a split-view / silent-rewind attempt is detected by the witness/consistency monitor; anchor proof present + verifiable in the bundle. → **Mechanism = independent witness cosigning** (Ed25519), behind a pluggable `AnchorPort` (TSA/public-endpoint can slot in later). New proof-only RFC 6962 §2.1.4 `verify_consistency_proof` (lets a party holding only STHs check consistency). `SoftwareWitness` cosigns an STH only if it consistently extends the last it saw → refuses a fork/rewind (`LedgerTamperError`). Cosignature embedded in the bundle `anchor` block; `verify_ledger_proof_bundle(..., trusted_witnesses={witness_id: pem})` verifies it (pinned, like D3-1). `GET /v1/ledger/witness-key`; `kernel.witness` wired into export. | code | DONE |
| ☑ D3-3 | **Crypto-review readiness.** Freeze the ledger/anchor surface and prep the K·02 review package (the audit itself is tracked in T-1). **DoD:** the anchored proof design + verifier are documented and frozen for review. → New reviewer-facing **`docs/operations/K02_REVIEW_PACKAGE.md`**: byte-exact serialization/hashing spec (leaf canonical JSON, 0x00/0x01 domains, STH `size‖root` message, witness `quaicu.witness.v1` NUL-delimited message, bundle wire format), full verifier spec (3 fail-closed properties), witness protocol incl. the empty-proof review question, 11-row threat→defense→test matrix (real test names), file inventory, run instructions. RFQ §2 scope updated to the D3-1/D3-2 surface (pinning, witness subsystem, aws_kms, proof-only §2.1.4 verifier); `[TAG]` filled = **`k02-review-v1`** (tag on the merged freeze commit). Freeze notice: changes under `core/ledger/`, `core/ports/anchor.py`, `core/regmap/export.py`, `adapters/ledger/`, `delivery/witness_app.py` need owner sign-off until T-1 lands. | code | DONE |

## Phase D4 — Multi-tenancy, isolation, packaging for both shapes

| ID | Task | Type | Status |
|----|------|------|--------|
| ☑ D4-1 | **Shared SaaS hardening (we operate).** Make `build_saas_app` behind `TieredKernelProvider` the production entrypoint: verify per-tenant RLS isolation, durable everything, `--workers > 1`, autoscaling, graceful shutdown. **DoD:** load test passes multi-worker; isolation test green; clean rolling deploy. → **Code complete** (2026-07-04 log): fixed the **critical multi-worker seal-loss bug** (optimistic seal linearization + advance-only STH — corrects D0-5's "workers>1 safe" claim, which missed the in-memory ledger tree); API-key/entitlement cross-worker fallbacks + TTL refresh; anchor-loop single-runner advisory-lock gate; **migration 017** (RLS on the 6 remaining tenant tables) + GUC-setting adapters; adversarial RLS suite (`tests/conformance/storage/test_rls_isolation.py`); load harness (`scripts/load_saas.py` + loadtest TOMLs); Terraform probes (/readyz startup, /health liveness) + resources/concurrency + opt-in Memorystore metering; rolling-deploy runbook. **Live go-live done (2026-07-06 log):** `quaicu_prod` migrated 013→017 (additive 014–016 while old rev served, then FORCE-RLS 017 after the D4-1 adapter build was serving); D4-1 image deployed as rev **00037-k9m** (zero downtime, continuity-probed); `/readyz` startup + `/health` liveness probes attached; RLS enforcement verified in prod (`'*'`→rows, unset→0, tenant→scoped). **Residuals (see log):** deploy was imperative `gcloud` (Terraform state not reconciled → drift), `k02-review-v2` re-tag on the merged commit still pending, integration conformance suite not re-run this session. | code+infra | DONE (shared plane LIVE · residuals noted) |
| ☐ D4-2 | **Dedicated single-tenant (customer-operated).** Per-customer GCP-project Terraform (reuse `deploy/terraform/gcp-enterprise/`), **customer-held Cloud KMS** signing root + BYO-keys, install/upgrade/rotate runbooks, support model. **DoD:** a dedicated instance stands up from IaC in a clean project with a customer KMS key; runbooks validated by a dry-run. | infra | TODO · blocked-by D3-1 |
| ☐ D4-3 | **Residency parameterization.** Region as a deploy-time parameter (reuse `regions/{eu,india,gulf}.tfvars`); residency matrix doc; opt-in zero-egress (VPC-SC) perimeter. **DoD:** deploy to a non-US region from IaC; residency matrix published; zero-egress validated for one zone. | infra | TODO · blocked-by D4-1 |

## Phase D5 — Identity, billing, operability

| ID | Task | Type | Status |
|----|------|------|--------|
| ☐ D5-1 | **SAML 2.0 adapter.** New SAML SSO adapter alongside `adapters/identity/oidc.py`. **DoD:** SAML login works against a test IdP; assertions mapped to actor/roles; tenant-isolated. | code | TODO |
| ☐ D5-2 | **Entra/Azure AD integration + certification.** OIDC/SAML against Entra; pursue certification (pairs with the Teams channel D1-3). **DoD:** Entra SSO verified; certification submission prepared. | code+human | TODO · blocked-by D5-1 |
| ☐ D5-3 | **Live Razorpay.** Complete KYC + provision the absent `RAZORPAY_WEBHOOK_SECRET`; enable `[billing.razorpay]`. Enterprise stays invoice/contract. **DoD:** a live ₹ payment + webhook tier-flip works on staging; webhook signature verified. | infra+human | TODO |
| ☐ D5-4 | **Observability + status (operated plane).** Log-based metrics + alerts + uptime check; `status.` page; on-call rotation. **DoD:** alerts fire on synthetic failure; status page live; rotation staffed. | infra | TODO · blocked-by D4-1 |
| ☐ D5-5 | **Resilience.** Run the **DR restore test**; stand up the retention-locked **WORM** bucket; trust-center page. **DoD:** a restore drill meets the (per-contract) RTO/RPO and is documented; WORM bucket live; trust center published. | infra | TODO · blocked-by D5-4 |

## Phase D6 — Acceptance (definition of done = ALL of these)

| ID | Task | Type | Status |
|----|------|------|--------|
| ☐ D6-1 | **Live pilot end-to-end.** One real tenant: an agent governed end-to-end → sealed → proof bundle **externally verified against the pinned anchor**. **DoD:** the full flow runs on both a dedicated instance and the shared plane. | infra | TODO · blocked-by D2-1, D3-2, D4-2 |
| ☐ D6-2 | **External security review passed.** Independent pen-test + security review clean (booking tracked in T-3). **DoD:** report received; criticals/highs remediated. | human | TODO |
| ☐ D6-3 | **Supply-chain + resilience green.** CI/SBOM/signed-images/vuln green **+** load test **+** DR restore test passed. **DoD:** all gates green in one release candidate. | infra | TODO · blocked-by D0-3, D4-1, D5-5 |

---

## Tracked non-code workstream (parallel; we sequence, others execute)

| ID | Item | Status |
|----|------|--------|
| ☐ T-1 | **Commission K·02 ledger crypto review** (the proof anchor depends on this for credibility). RFQ is send-ready (`docs/operations/CRYPTO_REVIEW_RFQ.md`, scope pinned to tag `k02-review-v1` + `K02_REVIEW_PACKAGE.md`). Remaining brackets are owner's: timeline `[N]`, budget, NDA, contacts → then send to 2–3 firms in parallel. | TODO · **actionable now** (D3-3 done) |
| ☐ T-2 | **SOC 2** (Type I → II observation window) — start the clock. | TODO |
| ☐ T-3 | **Independent pen-test** — book a firm (SoW drafted, `docs/compliance/PENTEST_SOW.md`). | TODO · feeds D6-2 |
| ☐ T-4 | **DPA/MSA counsel sign-off** (starters drafted under `docs/legal/`). | TODO |
| ☐ T-5 | **RBI/DPDP + credit-lending pack legal/CRO review** before activation in a real tenant; the console legal "draft" banner stays until counsel clears it. | TODO |

---

## Reuse (do not rebuild)
- **Email:** existing Resend sender (signup OTP) → D1-2. **Async approval:** `defer_gate` + `resume_after_approval` already in `core/lifecycle/engine.py` → D1-1. **Durable state:** `PostgresEntitlementRepository` + Postgres ledger/policy/HITL adapters → D0-5/D4-1. **IaC:** `deploy/terraform/gcp-{saas,enterprise}/` + region presets → D4-*. **Verifier scaffolding:** `core/regmap/export.py` → D3-*. **CI base:** `cloudbuild.yaml` → D0-3. **Billing:** `adapters/billing/*` → D5-3.

## Open items to resolve during execution
- D3-2 anchor mechanism choice (TSA vs witness service vs public endpoint).
- Confirm GCP Marketplace *metered* billing stays deferred (listing-readiness only).
- Explicitly out of scope this program: non-Python SDKs, rule-builder UI, multi-step SOP authoring, Slack/Jira HITL.

---

## Progress Log
_Append dated entries as items move. Format: `- [YYYY-MM-DD] (who) ID — what changed`_

- [2026-07-06] (claude) **D4-1 shared plane LIVE in production — deploy + `quaicu_prod` migration + probes, zero downtime.** Completed the live go-live tail of D4-1 (prior session left "code done · live verification pending"). **(1) Root-caused the failed prior deploy:** the first D4-1 revision (00035) crash-looped on `column "password_hash" does not exist` — `quaicu_prod` was at alembic **013**, but any post-D1-5 image reads `quaicu_members.password_hash` (mig 015) during startup hydration (would have bitten ANY deploy since 2026-07-02, not D4-1-specific). **(2) Staged migration respecting 017's live-ordering hazard:** verified against the migration bodies (not notes) that 014–016 are additive (`ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`) → applied 013→016 while the **old** rev 00034 kept serving (safe). Then deployed a fresh D4-1 revision (00035 is immutable/dead — Cloud Run won't retry a failed revision), which booted clean → **00036-qfw** serving 100%. **Only then** applied **017** (FORCE RLS on the 6 remaining tenant tables) — the migration's own docstring warns FORCE RLS blanks any read that doesn't set `app.current_tenant`, and I verified in code that all four adapters touching those tables (`account`/`entitlements`/`hitl`/`erasure`) set the GUC per-txn — `_run(tenant="*")` sentinel for genuinely cross-tenant reads incl. API-key auth discovery, owning tenant for writes — before applying it. **(3) Verified RLS enforcing in prod** as the app's own role: `'*'`→all rows (9 keys/6 accounts), **unset→0** (proves FORCE RLS is real), specific tenant→scoped (1/1). **(4) Probes:** added `startupProbe` GET `/readyz` (readiness-gated: 503 until hydration done, so Cloud Run won't route to an un-hydrated instance) + `livenessProbe` GET `/health` via `gcloud run services replace` (no CLI flags for probes) → rev **00037-k9m**; required enabling `cloudresourcemanager.googleapis.com` (which `run deploy` doesn't need but `services replace` does). **(5) Zero downtime:** continuity `/health` probes 200 across all three rollouts (32, 56, plus a clean 20/20 post-migration). **Also:** requirements-gcp.lock had been regenerated on Windows during MCP work and pinned pywin32 → broke every Linux image build since 2026-07-03; fixed on `fix/d4-1-load-harness` (regen for linux/py3.12) alongside the load-harness provisioning fix. **Residuals (open):** (a) the live deploy was imperative `gcloud`, not `terraform apply` — Terraform state now drifts from reality; reconcile (import/apply) before treating IaC as source of truth. (b) The `k02-review-v2` re-tag (for the optimistic-seal-linearization frozen-surface delta) still needs placing on the merged commit. (c) The integration conformance suite (`pytest tests/conformance/storage -m integration`) was not re-run this session (relied on the prior session's green run + this session's live RLS verification). DoD (load multi-worker · isolation green · clean rolling deploy) met; downstream D4-3/D5-1/D5-4/D6-3 unblock.
- [2026-07-04] (claude) **D4-1 code complete — shared SaaS hardening (multi-worker correctness + RLS everywhere + load/isolation harnesses + IaC).** Planning exploration found the D0-5 "workers>1 safe" claim **wrong for the ledger**: `TrustLedger` keeps a per-process tree + seq counter, and `adapters/ledger/postgres.py` inserted with `ON CONFLICT DO NOTHING` — two workers racing a tenant's seq **silently dropped one seal** (both reported sealed) and the last-writer-wins STH upsert could persist an STH inconsistent with the entries (accidental same-size fork — exactly what the D3-2 witness alarms on). **(1) Optimistic seal linearization** (frozen-surface delta, → re-tag `k02-review-v2`; RFQ unsent so re-freeze is cheap): a seal succeeds iff its INSERT inserted; conflict → new `LedgerSequenceConflictError` → `pop_last` → `_rehydrate_tenant` (new `load_tenant_entries`) → recompute+re-sign+retry (≤3, then HALT, F-03); STH upsert advance-only (witness-store pattern); memory repo mirrors; `test_concurrent_seal.py` (8 — two ledgers over one repo: no-loss interleave, STH==recomputed root, prefix-consistent proofs after conflict, stale-STH rejection, exhaustion HALT, benign replay). Each worker's tree = a consistent PREFIX of the durable log (never a fork). K02_REVIEW_PACKAGE §2.5 + §8 delta + threat row 12; RFQ tag → **k02-review-v2** (tag to be placed on the merged commit). **(2) Cross-worker auth/entitlements:** `find_api_key` (cache→repo fallback + `QUAICU_APIKEY_TTL` 60s revocation propagation; repo `get_api_key`/`get_account_by_tenant`), auth middleware runs `resolve_principal` off-loop via `to_thread`; `EntitlementStore.find_plan` + `engine.ensure_loaded` at the auth edge + periodic re-hydrate task (`QUAICU_ENTITLEMENTS_REFRESH` 60s). Tests: `test_cross_worker_auth.py` (6), `test_cross_worker_plans.py` (4). **(3) Anchor loop:** pg advisory try-lock single-runner gate (`delivery/api/leader.py`) — N workers, one pass per cadence; also fixed a latent D3-2 bug (a stray `@asynccontextmanager` decorated `_witness_kernels` instead of `lifespan`, killing the loop's iteration). **(4) Migration 017:** 007-style RLS (strict writes + `'*'` read sentinel + FORCE) on quaicu_{customer_plans,accounts,api_keys,members,shred_keys,approvals}; account/entitlements/hitl/erasure adapters now set the tenant GUC per txn (`'*'` only for genuinely cross-tenant reads); documented no-RLS: policies (no tenant_id, global) + witness_state (infra). Deploy order: adapter build BEFORE 017. **(5) Adversarial isolation suite** `tests/conformance/storage/test_rls_isolation.py` (9 tables × read/write/insert-forgery/no-GUC/sentinel + FORCE assertion + adapter e2e). **(6) Load harness** `scripts/load_saas.py` + `kernel.loadtest.{saas,shared}.toml` (memory_signed_ledger now accepts the durable Postgres store → real seal path without KMS): 2 tenants × 50 clients × 60s vs `--workers 4`; verdict from the DB (dense seqs, count==seals, recomputed root==STH, no cross-tenant rows, 0×5xx/401, p99<750ms). **(7) IaC/docs:** gcp-saas Terraform gets /readyz startup-probe (readiness-gated rollout) + /health liveness + cpu/mem + `startup_cpu_boost` + `max_instance_request_concurrency` + timeout + opt-in `enable_redis_metering` (Memorystore→REDIS_URL, precondition on VPC connector); `entrypoint_saas.main()` honors KERNEL_WORKERS; DEPLOY_CLOUD_RUN §8 rolling-deploy runbook + exact-vs-approximate (×N) semantics. Suite green + ruff clean. **Remaining for DoD (user-run):** 017 on test DB → `pytest tests/conformance/storage -m integration` → load run → terraform plan/apply → rolling deploy → tag `k02-review-v2`.
- [2026-07-04] (claude) **D3-3 DONE — crypto-review readiness; Phase D3 complete.** Verified D3-2 in code first (per the verify-against-code rule): full unit suite **1172 passed**, ruff clean; DoD spot-checks green (fork/rewind refusal in `SoftwareWitness.cosign` via proof-only §2.1.4; rewind-caught-after-restart with the durable store; anchored-bundle verify/reject cases in `test_export.py`). Committed the previously-uncommitted D3-2 follow-ups (remote witness service etc.). Then: **(1) Review package** `docs/operations/K02_REVIEW_PACKAGE.md` — the reviewer-facing frozen spec: byte-exact serialization (canonical-JSON leaf bytes, 0x00/0x01 domain separation, STH message `tree_size(8B BE)‖root`, witness message `quaicu.witness.v1\x00 + NUL-joined fields`, bundle `quaicu.ledger-proof/1.0`), the 3-property fail-closed verifier spec, the witness protocol (incl. flagging the **empty-proof path** in `anchor_current_sth` as an explicit review question), an 11-row threat→defense→test matrix citing real test names, the file inventory, run instructions, and honest out-of-scope (collusion-from-genesis, verifier record compromise, witness availability). **(2) RFQ updated** (`CRYPTO_REVIEW_RFQ.md`): scope now includes key pinning (D3-1), the witness subsystem (D3-2), `aws_kms.py`, and the proof-only consistency verifier; `[TAG]` filled = **`k02-review-v1`**. **(3) Freeze:** tag `k02-review-v1` on the merged commit; freeze notice in the package (ledger/anchor/export/witness files need owner sign-off until T-1 lands). **T-1 is now actionable** — owner fills timeline/budget/NDA/contacts and sends to 2–3 firms (~6–8 wk lead; longest pole to regulated GA). **Next: Phase D4** (D4-1 shared SaaS hardening / D4-2 dedicated single-tenant — both unblocked).
- [2026-07-04] (claude) **D3-2 follow-ups DONE — remote witness service + durable state + periodic anchoring.** Turned the in-process witness (which a kernel compromise owned + which forgot on restart) into a real, independent defense. **(A) Durable + monotonic state, stable key:** `WitnessStateStore` protocol + `InMemoryWitnessStateStore` (`core/ledger/anchor.py`); `PostgresWitnessStateStore` (`adapters/ledger/witness_store_postgres.py`, migration 016 `quaicu_witness_state`) with an **advance-only** upsert (`ON CONFLICT … WHERE EXCLUDED.tree_size >= stored`) so a rewound size can't overwrite the high-water mark. `SoftwareWitness` refactored to take `private_key_pem` (a **stable** key that survives restarts → pins stay valid) + a `state_store` (replaces the in-memory dict). **(B) Out-of-process service + HTTP client:** `delivery/witness_app.py` `create_witness_app` (`POST /cosign` [auth; 409 on fork/rewind], `GET /witness-key` [public], `GET /last-seen/{tenant}`, `/health`) + `delivery/entrypoint_witness.py` (`quaicu-kernel-witness`, its own key/DB/token from env); `adapters/ledger/http_witness.py` `HttpWitness` (`AnchorPort` over sync httpx like openbao; 409→`LedgerTamperError`; caches the witness key) — `anchor_current_sth` is unchanged (works against any AnchorPort). **(C) Periodic anchoring:** `anchor_all_tenants` (`core/ledger/anchor.py`, tamper on one tenant logs CRITICAL + continues) + a lifespan background loop in `create_app` gated by `anchor_interval`, off the event loop via `to_thread`, cancelled on shutdown; `TrustLedger.tenants()` accessor. **(D) Config:** `from_config._build_witness` (`[adapters] witness = "http_witness"` + `[witness] base_url/token`) sets `Kernel.witness`; `build_saas_app` reads `[anchor] interval_seconds`; commented `[witness]`/`[anchor]` in kernel.shared/saas.toml; `quaicu-kernel-witness` script. Tests: `test_witness_state.py` (monotonicity + **rewind-caught-after-restart** with durable state, and the contrast that a fresh store misses it), `test_witness_service.py` (from_config wiring, public witness-key, HTTP cosign roundtrip+verify, auth-required, 409→TamperError, anchor_all_tenants). Unit suite **1171 passed** (+8), ruff clean. LEDGER_TRUST_MODEL §3 updated (deploy the witness separately). **Deferred (owner):** TSA + public-append-only anchors; HA witness load-testing. **Phase D3 remaining: D3-3** (crypto-review readiness — freeze the ledger/anchor surface).
- [2026-07-04] (claude) **D3-2 DONE — external witness (cosigning) + consistency monitor + anchor proofs.** Defends against split-view / silent-rewind (a compromised kernel presenting two histories or rewinding). **Mechanism decision:** independent **witness cosigning** (Ed25519), behind a pluggable `AnchorPort` (`core/ports/anchor.py`) so RFC-3161 TSA / public-append-only can be added later without touching callers. **Crux:** the existing consistency verifier needed the full leaf list, so a witness/auditor (STHs only) couldn't use it — added a **proof-only RFC 6962 §2.1.4** `verify_consistency_proof(old_root, new_root, old_size, new_size, proof)` to `core/ledger/merkle.py` (canonical CT/Trillian algorithm; reconstructs both roots from the proof alone; validated by round-trips across n=1..32 + fork/rewind rejection). **`SoftwareWitness`** (`adapters/ledger/witness.py`): independent Ed25519 key + per-tenant last-cosigned STH; on `cosign(tenant, sth, consistency_proof)` verifies the proof shows `sth` extends its last-seen root → cosigns, else raises `LedgerTamperError` (fork / same-size fork / rewind all refused). Orchestration `core/ledger/anchor.py::anchor_current_sth`. **Surfaced in the bundle:** `build_ledger_proof_bundle(..., witness_cosignature=…)` embeds an `anchor` block; `verify_ledger_proof_bundle(..., trusted_witnesses={witness_id: pem})` verifies the cosignature (pinned like D3-1, attests same size+root as the STH; omit → anchoring skipped, integrity+authenticity still pass). **Wiring:** optional `Kernel.witness` (from_parts) → `export_ledger_proof` anchors on export (a tamper raises, so no bundle for a compromised log); `GET /v1/ledger/witness-key` publishes `{witness_id, public_key_pem, algorithm}` to pin; `/export/verify` pins the witness when wired. Tests: `tests/unit/ledger/test_consistency_proof.py` (proof-only: round-trip all sizes, edge cases, tamper, forked-history), `test_witness.py` (cosign growth + verify, refuse rewind, refuse same-size fork), `test_export.py` (+6 anchored cases), route tests (witness-key + anchored roundtrip + 503). Unit suite **1163 passed** (+14), ruff clean. LEDGER_TRUST_MODEL doc extended (§3 Anchoring). **Follow-ups:** remote/HTTP witness (true out-of-process independence), TSA/public-endpoint adapters, periodic-anchor scheduler, durable witness last-seen state. **Next: D3-3** (crypto-review readiness — freeze the ledger/anchor surface for the K·02 review). *(On branch `feat/d3-1-key-pinning`.)*
- [2026-07-04] (claude) **D3-1 DONE — ledger proof-bundle key attestation / pinning.** Closed the trust hole in the offline verifier: `verify_ledger_proof_bundle` used to check the STH signature against the **public key embedded in the bundle itself** (`core/regmap/export.py:213`), so a forged bundle carrying its own keypair passed. Now `verify_ledger_proof_bundle(bundle, *, trusted_keys: {key_id → PEM} | None)` splits the two properties: **integrity** (inclusion proofs recompute the signed root) stays self-contained; **authenticity** verifies the signature against the caller-**pinned** key selected by the bundle's `key_id` — never the embedded one. Fail-closed: `trusted_keys=None` → fail ("no pinned trust anchor"); `key_id` not pinned (forged/unknown) → fail; sig-mismatch → fail; embedded key ≠ pinned → fail (tamper signal, embedded key is now advisory only). New `trusted_keys_from_signer(signer)` helper; `public_key_pem` added to the `TreeSigner` protocol (`core/ledger/signer.py`). **Publish-to-pin:** new `GET /v1/ledger/signing-key` → `{key_id, public_key_pem, algorithm}` (ed25519/ecdsa-p256) that a regulator records + pins once at onboarding; `POST /v1/ledger/export/verify` now pins against the tenant kernel's own signer (answers "did we sign this?", now auth-required). Trust-model doc `docs/operations/LEDGER_TRUST_MODEL.md`. Tests: `tests/unit/regmap/test_export.py` — the 5 DoD cases (correct pin → ok; no anchor → fail; forged key → fail; swapped embedded key → fail; wrong pinned pem → sig fail) + a missing-embedded-key-still-verifies case; adapter signer tests + route tests (signing-key publish, verify requires bearer) updated to pin. Unit suite **1149 passed** (+6), ruff clean. **Next: D3-2** (external witness / anchoring + continuous consistency monitoring). *(On branch `feat/d3-1-key-pinning` off merged main.)*
- [2026-07-04] (claude) **A — hosted MCP endpoint served on the SaaS plane (config + image) — DONE.** Made `/mcp` mountable on the deployed shared plane (previously `entrypoint_saas.py → build_saas_app(_config)` never wired it, and the image didn't even ship the MCP SDK, so `kernel.quaicu.org/mcp` 404'd). **(1) Config-driven:** new `delivery/sdk/mcp_config.py` `build_mcp_gateway(config)` (mirrors `build_usage_meter`) — `[mcp] enabled=true` → a BYO-downstream `GovernedMCPProxy(name, default_policy)`, else None; fail-fast `ValueError` if enabled but the `mcp` extra is missing. `build_saas_app` now does `mcp_proxy = mcp_proxy or build_mcp_gateway(config)` (explicit param still wins) → `create_app` mounts `/mcp`. Registered-tools mode stays the programmatic `mcp_server=` param (needs code handlers). **(2) Image ships the SDK:** regenerated `requirements-gcp.lock` via `uv pip compile … --extra gcp --extra mcp --generate-hashes --constraint requirements.lock` → now hash-pins `mcp==1.28.1` (+ httpx-sse, sse-starlette); Dockerfile `--require-hashes` install unchanged, comment updated (gcp + mcp). **(3) Config section:** commented `[mcp]` block in `delivery/docker/kernel.saas.toml`, **enabled=false by default** (opt-in), with the per-tenant prereqs noted (register downstream at `PUT /v1/mcp/connection`; activate `mcp.<tool>` policies). Tests: `tests/unit/sdk/test_mcp_config.py` (3 — disabled→None, enabled→proxy, default_policy groups tools). Unit suite **1143 passed** (+3), ruff clean. README: "Turn it on (deployed SaaS plane)". **Operator go-live (not code):** set `[mcp] enabled=true`, deploy the image (now includes the extra), then per tenant: downstream connection + `mcp.<tool>` policies.
- [2026-07-03] (claude) **MCP per-argument policies + fixed the vestigial policy knob — DONE.** MCP tool calls are now governable by their **arguments** via CEL, and the previously-dead `policy` knob works. **#1:** `govern_tool_call` was calling the governed closure as `governed(arguments=args)`, so the action payload was `{"arguments": {...}}` and the CEL activation (one-level flatten + stringify nested dicts) only exposed `payload_arguments` (a stringified blob) — per-argument conditions were impossible. Added **`Kernel.govern_action(*, action_type, actor, payload, execute, …)`** (delivery/sdk/kernel.py) — governs an action with an **explicit payload dict** (execute→seal, same fail-closed raises as `wrap`), reusing `resolve_profile`/`_build_action`/`engine.run`/`_raise_on_terminal_failure`. `govern_tool_call` now passes the **tool args as the payload directly**, so a CEL policy can condition on `payload_<field>` (e.g. `governs="mcp.wire_transfer", condition="payload_amount > 10000"`); no reserved-kwarg collision (an arg named `actor` is just `payload_actor`). **#2:** `governs` matches exact-or-`*` only (no `mcp.*` prefix), so mapping tools to a shared **action type** is the only way to write one policy for a group — `govern_tool_call`'s `policy` param (was ignored: `wrap` used `action_type or policy` with action_type hardcoded) now **selects the action type** (`policy or f"mcp.{tool}"`); `GovernedMCPServer`/`GovernedMCPProxy` `default_policy` default flipped `"mcp.tool"`→`None` (preserves the per-tool `mcp.<tool>` default, but a set value now takes effect). Consistent with the SDK where `guard(policy=…)` *is* the action type. Tests: `tests/unit/mcp/test_arg_policies.py` (2, **real PolicyEngine**) — amount>10000 denied / amount=5000 allowed (proves `payload_<field>` fires); a tool with `policy="mcp.tool"` governed by a `governs="mcp.tool"` policy (proves the knob routes the action type). Unit suite **1140 passed** (+2), ruff clean; existing MCP tests green (params kept). README: "Writing policies for MCP tools". **(This was task B; task A = wire `/mcp` into the SaaS entrypoint so kernel.quaicu.org serves it — next.)**
- [2026-07-03] (claude) **BYO-downstream MCP follow-ups (pooling · SSE · console UI) — DONE.** Closed the three flagged follow-ups. **(1) Safe HTTP keep-alive pool:** `delivery/mcp/downstream.py` now reuses a **per-worker shared `httpx.AsyncClient`** (keyed by event-loop id) passed to the mcp client via its `httpx_client_factory`, wrapped in a `_NoCloseClient` so the mcp client's `async with factory() as c` doesn't close the shared client — TCP/TLS stays warm across calls; `aclose_shared_clients()` closes them on app-lifespan shutdown (wired in `create_app` proxy mode). **MCP *sessions* stay fresh-per-request by design** (anyio streams are task-bound → unsafe to pool in a stateless multi-worker server) — documented in code + README. **(2) SSE downstream transport:** `HttpDownstream._session` selects `sse_client` (2-tuple) vs `streamablehttp_client` (3-tuple) by `conn.transport`; `/v1/mcp/connection` PUT accepts + validates `transport` (`streamable_http`|`sse`, 422 otherwise). **(3) Console MCP-gateway page:** new `console/src/pages/MCPGateway.tsx` (mirrors AIGateway — downstream URL, transport select, encrypted auth value, name; masked connected-card; "point your agent at `<origin>/mcp`" snippet) + `getMCPConnection`/`setMCPConnection`/`deleteMCPConnection` client methods + `MCPConnection{Status,Body}` types + `/mcp` nav link & route in `App.tsx`. Tests: `test_downstream.py` extended (SSE path, shared-factory passed, `_NoCloseClient` no-op close), `test_mcp_connection_routes.py` (+SSE roundtrip, +invalid-transport 422). Unit suite **1138 passed** (+4), ruff clean; console **typecheck + build green**. README updated (SSE + pooling).
- [2026-07-03] (claude) **BYO-downstream-per-tenant hosted MCP (full AI-gateway parallel) — DONE.** The hosted `/mcp` endpoint now supports **BYO downstream**: each tenant registers their **own** downstream MCP server and the endpoint mirrors + governs + forwards *their* tools (previously: operator-registered tools only, same catalogue for all tenants). This is the MCP twin of `/v1/ai/connection` + the gateway. New: **`MCPConnection`** model (`core/account/model.py`) + `AccountEngine.set/get/mcp_connection_status/clear_mcp_connection` (mirrors `AIConnection`; secret AES-GCM-encrypted at rest via the existing `_encrypt_secret`, status masks it); **`/v1/mcp/connection`** REST routes (GET/PUT/DELETE, mirror `ai_gateway`); **`delivery/mcp/downstream.py`** `HttpDownstream` (satisfies the proxy's `Downstream` protocol by opening a fresh `streamablehttp_client`+`ClientSession` per call — connect→initialize→list/call; stateless, pooling deferred); **multi-tenant `GovernedMCPProxy`** (`ProxySession(kernel, actor, downstream)` + `bind_session_resolver`; `_resolve`/`_govern`/`list_tools` per-request, single-tenant/stdio unchanged); **generalized `build_streamable_http_app(governed, *, resolve_from_request)`** (was `authenticator=`) + **`build_proxy_resolver(authenticator, account_engine)`** (authenticate → `get_mcp_connection(tenant)` → `HttpDownstream`; fail-closed error if no downstream). Wired **`create_app(mcp_proxy=…)`** / **`build_saas_app(mcp_proxy=…)`** (mutually exclusive with `mcp_server`, mounts `/mcp`). Isolation: a tenant with no downstream is told to register one; a tenant only ever reaches their own downstream + seals to their own ledger. Tests: `tests/unit/account/test_mcp_connection.py` (6), `tests/unit/api/test_mcp_connection_routes.py` (2 — secret masked), `tests/unit/mcp/test_downstream.py` (3 — fake mcp client), `test_multitenant_proxy.py` (2 — forward-to-resolved-downstream + isolation + fail-closed), `test_http_proxy.py` (4 — resolver fail-closed/build, create_app mutual-exclusion, 401 gate). Unit suite **1134 passed** (+16), ruff clean; D2-1 stdio + prior hosted-server path green. README updated (two hosted modes). **Follow-ups:** downstream connection pooling; SSE downstream transport; console UI for the MCP connection.
- [2026-07-03] (claude) **Hosted multi-tenant MCP endpoint (extends D2-1) — DONE.** Made MCP a **non-code-exposing hosted surface** (the "Stripe model", MCP analogue of the BYO AI gateway): one authenticated **Streamable-HTTP** endpoint at `/mcp` serves every tenant; a tenant's agent connects with its **API key**, and each tool call is governed by *that tenant's* policies and sealed to *that tenant's* ledger. Previously MCP was **stdio-only** → client launched the package locally → full source on their machine. New: **`delivery/mcp/auth.py`** `MCPAuthenticator` (`Bearer qk_…` → `resolve_principal` → `(tenant, actor via subject/roles, kernel via provider.kernel_for)`; fail-closed `ApiKeyInvalidError`); **`delivery/mcp/http.py`** `build_streamable_http_app` (wraps `StreamableHTTPSessionManager(json_response=True, stateless=True)` → no SSE/sticky sessions, scales with `--workers>1`; 401s a missing bearer early); **multi-tenant mode on `GovernedMCPServer`** (per-request `session_resolver` → `bind_session_resolver`; `_resolve_identity` replaces the fixed `self._kernel/_actor`, also gating `list_tools`; single-tenant/stdio path unchanged). **Identity threading crux:** stateless mode runs the low-level Server in a *separate task* so a self-managed ContextVar won't cross it — instead the handler reads the transport-attached Starlette request via `low_level.request_context.request` (verified: streamable_http sets `ServerMessageMetadata(request_context=Request(scope,…))` → mapped to `RequestContext.request`) and authenticates there, robustly per request. Wired into **`create_app(mcp_server=…)`** (mounts `/mcp` **outside** `/v1` so the tenant-extracting `ApiKeyAuthMiddleware` doesn't intercept a raw `qk_` key; enters `manager.run()` in the lifespan) and **`build_saas_app(config, mcp_server=…)`** (operator provides the tools-only server in code — TOML can't carry handlers). Tenant isolation = per-request `(tenant, actor, kernel)` (F-07 boundary, same as the shared gateway). Tests: `tests/unit/mcp/test_http_auth.py` (3), `test_multitenant_server.py` (2 — per-tenant seal + isolation + fail-closed resolver, transport-free via stub resolver), `test_http_endpoint.py` (2 — mount + lifespan `run()` + 401 gate via TestClient). Unit suite **1118 passed** (+7), ruff clean; D2-1 stdio path green (single-tenant unchanged). README updated with the hosted section. **Follow-up (out of scope):** BYO-downstream-per-tenant proxy (per-tenant downstream MCP connection config — the full gateway parallel); optional `MCP_INVOKE` scope; single-tenant hosted runner for dedicated deployments.
- [2026-07-02] (claude) **D2-3 DONE — Python SDK hardening; Phase D2 complete.** Hardened the SDK's error surface, approval ergonomics, typing, and docs around the D1-1 async-approval model. **(1) Typed, re-exported errors:** new `delivery/sdk/errors.py` `SdkUsageError(QUAICUError)` (`code=SDK_USAGE_ERROR`) — a *delivery-layer* subclass of the frozen root (no edit to `core/errors.py`, no parallel tree in core). `delivery/sdk/__init__.py` now re-exports the whole catchable surface: `QUAICUError`, `LifecycleError`, `Lifecycle{Denied,Halted,PendingApproval}Error`, `SdkUsageError` (the D1-1 note's "re-exported from the SDK" claim was previously false — confirmed & fixed). **(2) Consistent misuse errors:** the 7 ad-hoc `RuntimeError`/`TypeError` sites — "no actor in scope" in `guard`/`wrap`/`governed`/`governed_tool`/`GovernedProxy`, and "no adapter/HITL configured" in `generate`/`decide_approval` — now all raise `SdkUsageError`. Governance *outcomes* (`_raise_on_terminal_failure`: deny→Denied, halt→Halted, pending→PendingApproval) are unchanged. **(3) Approval ergonomics:** added `kernel.approve(handle_id, *, actor)` / `kernel.reject(...)` — thin typed wrappers over the existing `decide_approval` (which already runs `resume_approved`: execute→seal), driven off `exc.detail["handle_id"]` from a caught `LifecyclePendingApprovalError`. **(4) Typed (PEP 561):** added `core/py.typed`, `adapters/py.typed`, `delivery/py.typed` + `[tool.setuptools.package-data]`; **verified shipped in the built wheel**. **(5) Runnable example + docs:** `examples/sdk-approval-demo/{demo.py,README.md}` — self-contained (real PolicyEngine + in-memory HITL/ledger/events), prints allow→deny→approve→self-approval-block, imports the governance API + every exception from `delivery.sdk`; **rewrote** the "Coming soon" SDK reference (Exceptions & error-semantics table + Async-approval section) and tutorial. **Tests:** `tests/unit/sdk/test_sdk_error_semantics.py` (8), `test_approval_ergonomics.py` (3, incl. self-approval-block), `tests/unit/examples/test_sdk_approval_demo.py` (1 — runs the example → DoD "examples run green"); updated 5 existing tests that asserted the old `RuntimeError`/`TypeError` misuse types. Unit suite **1111 passed** (+12), ruff clean, example runs green, wheel builds with markers. Reused: `QUAICUError` root, `_raise_on_terminal_failure`, `decide_approval`/`resume_approved`, `Kernel.from_parts` + lifecycle fakes. **Phase D2 (interception surfaces) is now complete** (D2-1 MCP · D2-2 gateway tool-calls · D2-3 SDK). Next: Phase D3 (proof layer — D3-1 key pinning).
- [2026-07-02] (claude) **D2-2 DONE — gateway tool-call + embeddings governance.** The BYO AI gateway now governs an agent's **provider tool calls**, not just chat. In a passthrough gateway the tool call the model asks to make appears in the *response*, so (1) **shims surface it in OpenAI shape** — `AnthropicShim` translates `tools`/`tool_choice`, assistant `tool_calls`→`tool_use`, `role:"tool"`→`tool_result` on the way out and `tool_use`→`tool_calls` on the way back (response **and** stream via `input_json_delta`); `BedrockShim` maps `tools`→`toolConfig`, tool_calls/results→`toolUse`/`toolResult`, and `toolUse`→`tool_calls` (response + `converse_stream`); OpenAI/Azure/Vertex are already OpenAI-shaped (passthrough). (2) The route (`ai_gateway.py`) seals **each response tool call as an `ai.tool_call` action** through the existing `kernel.check(record=True)` seal path — payload is a **non-PII summary** (tool name + arg length + call id), same discipline as `ai.chat`; response args still rehydrate via `_rehydrate_obj`. **Fail-closed per owner decision:** any denied tool call **fails the whole response** — non-stream → `403 GOVERNANCE_DENIED`; **streaming** → a new `_govern_tool_call_stream` passes text/role/finish chunks live but **buffers tool_call deltas**, accumulates by index, seals at stream end, flushes them only if all allowed, else emits an SSE `GOVERNANCE_DENIED` error event and stops (governance runs **outside** the translate try/except so an unexpected error can't fall through to raw passthrough). (3) New governed **`POST /v1/ai/embeddings`** (`ai.embed` seal + PII-masked input + budget; providers without an OpenAI-shaped embeddings API — Anthropic/Bedrock — → `501 NOT_SUPPORTED` via `hasattr(shim,"embeddings_request")`). Tests: `tests/unit/api/test_ai_tool_governance.py` (6: OpenAI/Anthropic/Bedrock allow+deny, streaming allow-after-text + deny-error-event) + `test_ai_embeddings.py` (6) + shim-level translations added to `test_ai_providers.py`. Unit suite **1099 passed** (+20), ruff clean. Reused: `kernel.check` seal, `masking_port`/`MaskingContext`/`_rehydrate_obj`, `ai_budget`, `get_shim`/`UpstreamRequest`, the `_FakeClient`/`_build` harness, `_STOP_REASON`/`_BEDROCK_STOP`. **D2-3 (SDK hardening) is now unblocked** (its blocker D1-1 is done).
- [2026-07-02] (claude) **D2-1 DONE — MCP governance server.** New `delivery/mcp/` surface that governs an agent's MCP tool calls through the kernel (policy → HITL → K·02 seal), fail-closed. **Reusable core** `govern_tool_call`/`ToolOutcome` (`governance.py`) is a thin wrapper over `kernel.wrap` with **no `mcp` dependency** — allow→execute+seal, deny/halt→blocked (tool never runs), require_approval→durable PENDING + approval handle. **`GovernedMCPServer`** (register tool handlers + per-tool policy) and **`GovernedMCPProxy`** (front a downstream MCP server, mirror+govern+forward — no agent/tool code change) build on it using the official `mcp` SDK (lazy import; new `[mcp]` extra + `quaicu-kernel-mcp` proxy entrypoint); shared `render.to_call_result` maps outcomes to `CallToolResult` (blocked→`isError`, PENDING carries `_meta.handle_id`). README quickstart. **Bonus:** surfaced + fixed a latent D1-1 bug — `kernel.wrap` was missed by the defer-gate change and still false-DENYed `require_approval`; fixed (`delivery/sdk/kernel.py`) + added a regression test. Tests: `tests/unit/mcp/{test_governance,test_server}.py` (12; server/proxy gated on `mcp` which is installed). Unit suite **1079 passed**, ruff clean, `delivery.mcp` imports green.
- [2026-07-02] (claude) **D1-5 DONE — in-browser member maker/checker; Phase D1 complete.** Members can now log into the console and approve, not just via API key. Added `Member.password_hash` (migration 015, additive) + Postgres member SQL; `AccountEngine`: `mint_member_set_password_token` (sealed, expiring), `set_member_password`, `authenticate_member` (global email lookup + password disambiguation, no enumeration), `mint_member_session` (session JWT `sub=member_id` so the member is the sealed governance actor, `roles=actor_roles_for(role)`, `scopes=role_scopes(role)` — COMPLIANCE ⇒ `compliance` + `APPROVAL_DECIDE`). `/v1/auth/login` tries account then member; new `/v1/auth/set-password`; `POST /v1/members` invite emails a signed set-password link (`CONSOLE_BASE_URL`, best-effort/log-only fallback). Console: new `SetPassword` page + `/set-password` route + `api.setPassword` (the email/password login form, OIDC login, and Approvals page already existed). IdP handled via the existing OIDC path (SAML is D5-1). **Verified:** `tests/unit/account/test_member_login.py` + `tests/unit/api/test_member_approval.py` prove the full member login→approve round-trip seals `user:<member_id>` and SoD blocks a member approving their own action (409); unit suite **1067 passed**, ruff clean, console **typecheck + build** green. Migration 015 is trivial-additive (applies on next `alembic upgrade head`). **Phase D1 (human-approval loop) is now complete end-to-end:** async suspend (D1-1) → email/Teams push (D1-2/D1-3) → durable record (D1-4) → in-browser member decision (D1-5).
- [2026-07-02] (claude) **D1-4 DONE (code, reframed) — durable approval record + fail-closed send-then-persist (no worker).** Owner reframed the DoD's delivery worker away: **routing is fail-closed** (a send/outage error HALTs) and **a pending approval waits until a human decides** — so a persisted PENDING record already implies a delivered notification and there's nothing to retry → no background worker. Enriched `ApprovalRecord` (`core/hitl/model.py`) with `notify_channel`/`notify_target`/`resume_link`; refactored `InProcessHITLPort` into `_new_handle_id`/`_build_record`; email + Teams adapters now **send-then-persist** (notify first, persist only on success → no orphan PENDING on failure) and default to **`timeout_seconds=0` (no expiry)**. `PostgresApprovalStore` + migration 014 (additive nullable columns) persist the routing metadata; `core/ports/hitl.py` docstring updated. Tests extended (routing metadata present, `expires_at is None`, no-orphan-on-failure) + a live-PG round-trip test. Unit suite **1061 passed**, ruff clean. **Live-verified 2026-07-02:** migration 014 applied to GCP Cloud SQL + `tests/conformance/storage/test_shared_plane.py -m integration` → **4 passed** (incl. `test_approval_routing_metadata_survives_restart` — a fresh Postgres store reads back notify_channel/target/resume_link).
- [2026-07-02] (claude) **D1-3 DONE — Microsoft Teams HITL adapter.** New `MicrosoftTeamsHITLAdapter(InProcessHITLPort)` (`adapters/hitl/teams.py`) — same shape as the email adapter: inherits the store + resume flow, overrides `request_approval` to POST an Adaptive Card (two `Action.OpenUrl` approve/reject buttons) to a Teams incoming webhook. The buttons point at the **existing** D1-2 signed confirm-page flow (`ApprovalLinkSigner` + `/v1/approvals/link/{token}`), so the decision round-trip, single-use, tenant isolation (token encodes tenant; route enforces), and fail-closed decision path are shared — no new decision route. Fail-closed on non-2xx / transport error (HITLPortError → HALT). Config-selectable `hitl = "teams"` via `_build_teams_hitl` + commented example in `kernel.shared.toml`. Injectable `http_post` keeps tests network-free. MVP posts to one configured webhook (per-tenant webhooks are a later step). Tests: `test_teams_hitl.py` (4); full unit suite **1059 passed**, ruff clean.
- [2026-07-02] (claude) **D1-2 DONE — Email HITL adapter (signed single-use links).** New `EmailHITLAdapter(InProcessHITLPort)` (`adapters/hitl/email.py`) inherits the approval store + poll/approve/reject + the D1-1 resume flow, and overrides `request_approval` to email the configured approver an approve + reject link (Resend when `RESEND_API_KEY`, else log-only; a send failure raises `HITLPortError` → fail-closed HALT). Links are HMAC-signed + expiring via new pure-stdlib `core/hitl/links.py` `ApprovalLinkSigner` (reusable by D1-4's resume link). New `GET /v1/approvals/link/{token}` renders a confirm page (prefetch-safe) and `POST` commits → `decide_approval` → resume/deny; single-use enforced by the store (reused link → 409); tamper/expiry → 400; missing `QUAICU_APPROVAL_LINK_SECRET` → 503. Config-selectable `hitl = "email"` via `_build_email_hitl` in kernel.py + a commented example in `kernel.shared.toml`. Recipient is a configured compliance inbox for MVP (per-member routing is D1-5). Tests: `test_email_hitl.py` + `test_approval_links.py` (9); full unit suite **1055 passed**, ruff clean.
- [2026-07-01] (claude) **D1-1 DONE — async-by-default approval on the SDK path.** The SDK entry points (`guard`/`wrap`/`generate`/`governed*`) now call `engine.run(..., defer_gate=True)`, so a `require_approval` policy **suspends the action durably** (PENDING_APPROVAL) instead of the old poll-once → `TIMED_OUT` → false DENY. New `LifecyclePendingApprovalError` (core/errors.py; re-exported from the SDK) is raised on suspend, carrying `action_id` + the in-process `handle_id`. Approval resumes via the existing `decide_approval`→`resume_approved`→`resume_after_approval` (execute→seal→emit); reject DENIES; a real HITL-infra failure still HALTs (fail-closed). On the D0-5 durable plane the approval + action are Postgres-backed, so this resumes across instances/restarts. Fixed the now-stale caveat in `approvals.py`. Caveat: for `guard`/`wrap` the resume records the durable action payload + seals — it does not re-run the live Python closure (maker-checker record; live re-execution is D1-4/propose). Tests: `tests/unit/sdk/test_async_approval.py` (5); full unit suite **1046 passed**, ruff clean.
- [2026-07-01] (claude) **D0-5 DONE (code) — shared durable plane + upgrade = feature unlock** (owner reframe of the original "durable hot-path closure"). **5a:** new `delivery/docker/kernel.shared.toml` (one durable kernel, Postgres+KMS, `default_profile=standard`); `TieredKernelProvider.for_shared_saas`; `build_saas_app` → single `[plane] config`; `kernel.saas.toml`/Dockerfile updated; `TIER_MATRIX` STARTER→durable adapters (BUSINESS superset). **5b:** collapsing kernels removed the structural feature-wall, so added explicit fail-closed `max_policies` enforcement at `register_policy` (+ tenant-stamp so the count is non-bypassable; `QuotaExceededError`→429); confirmed no profile selector exists and left the BYO AI gateway open to all tiers (owner decision). **5c:** raised caps STARTER 5→200 / BUSINESS 200→1000; wrote real-Postgres integration proofs (`tests/conformance/storage/test_shared_plane.py`: cross-worker idempotency + restart durability + upgrade-is-metadata-only); ADR-0013 + DURABILITY_AUDIT Gap 1 marked RESOLVED + deploy docs updated. Unit suite **1041 passed**, ruff clean. **Live-verified 2026-07-01:** ran `tests/conformance/storage/test_shared_plane.py -m integration` against the GCP Cloud SQL instance (`quaicu-pg`, via the Auth Proxy on :5433, `alembic upgrade head` applied) → **3 passed** (cross-worker idempotency, restart durability, upgrade-is-metadata-only). One test-only fix during the run: `ActionState.EXECUTED`→`COMPLETED`.
- [2026-07-01] (claude) **D0-4 DONE — package listing/legal artifacts.** The repo-root LICENSE/README/SECURITY were already listing-grade, but the **distributable package** (`New/quaicu-kernel/`) shipped with none of the licensing metadata: no `LICENSE`, no `README.md`, and `pyproject.toml` had no `license` field + a placeholder inline `readme`. Added `New/quaicu-kernel/LICENSE` (verbatim mirror of the proprietary license, placeholders left for counsel/T-4) + a concise, accurate, listing-grade `New/quaicu-kernel/README.md` (what it is, install, **how to deploy** via the kernel.*.toml profiles + Terraform/Cloud-Run doc links; uses the real `Kernel.from_config`/`@kernel.guard` API, not the root README's stale `from_parts(api_key=…)` snippet). Wired `pyproject.toml` PEP 639: `readme="README.md"`, `license="LicenseRef-Proprietary"`, `license-files=["LICENSE"]`, `[project.urls]`, classifiers (incl. `Private :: Do Not Upload`), bumped `setuptools>=77`. De-staled `SECURITY.md`'s supply-chain bullet to match D0-3 (blocking scans + cosign/SBOM). **Verified:** `uv build` produces sdist+wheel; wheel METADATA carries `License-Expression: LicenseRef-Proprietary`, bundled `LICENSE`, and the markdown README long-description. No source code changed.
- [2026-07-01] (claude) **D0-3 DONE (config) — supply-chain CI graduated to blocking.** Edited `New/quaicu-kernel/cloudbuild.yaml`: pip-audit now blocking and audits **both** locks (base + gcp superset); Trivy now `--severity HIGH,CRITICAL --ignore-unfixed --exit-code 1` (blocking, matching the already-shipped `release.yml` gate); CycloneDX SBOM emitted every build (no `|| true`); push now captures the immutable digest; **new `cosign-sign` step signs the pushed digest + attaches a CycloneDX SBOM attestation using a Cloud KMS HSM key** (`gcpkms://`, `--tlog-upload=false` → sovereign, zero-egress/VPC-SC-safe; user chose KMS over keyless). Added `New/quaicu-kernel/.trivyignore` (documented-exception list, empty) and updated `docs/operations/VULN_MANAGEMENT.md` (modes → blocking; §3 marked complete; one-time KMS key + IAM setup documented). YAML validated; no report-mode remnants. **Not runnable locally** (Cloud Build is billable/remote) — operator must provision the KMS `cosign-signer` key + grant the Cloud Build SA `roles/cloudkms.signerVerifier`, set `_COSIGN_KMS_KEY`, then `gcloud builds submit`. Feeds D6-3 (supply-chain green).
- [2026-06-30] (claude) **D0-2 DONE — dependency lockfile reproducibility.** Found the base `requirements.lock` already existed (uv-compiled, hash-pinned) and the Dockerfile already installs it with `--require-hashes`. The one real gap: the BUSINESS-tier Cloud KMS dep was installed floating + un-hashed (`pip install google-cloud-kms`), breaking full reproducibility (its comment even said to fix this). Generated `New/quaicu-kernel/requirements-gcp.lock` via `uv pip compile pyproject.toml --extra gcp --generate-hashes --constraint requirements.lock` — a hash-pinned **consistent superset** of the base lock + the `gcp` extra (zero shared-dep drift, confirmed). Changed the Dockerfile builder to `pip install --require-hashes -r requirements-gcp.lock`. Base lock stays SDK-free by design (`aws`/`redis` extras not on the operated plane → left unlocked). Verified: lock resolves under `--require-hashes` on Python 3.12 (image interpreter) via uv dry-run; full `docker build` not run locally (Docker Desktop daemon down) but the build path is unchanged except the now-hashed gcp layer. Unblocks D0-3 (SBOM).
- [2026-06-30] (claude) **D0-1 DONE — state-of-durability audit.** Traced the operated plane (`build_saas_app` → STARTER + BUSINESS kernels from `kernel.{starter,business}.toml`). Result: **BUSINESS is durable on every correctness-critical store** (postgres action repo/policy/ledger + postgres HITL queue); **entitlements + accounts durable** and `PostgresEntitlementRepository` genuinely wired (`entitlements_config.py:56`). Gaps, all code-cited in `docs/operations/DURABILITY_AUDIT.md`: (1) STARTER fully in-memory on the hot path — `_InMemoryActionRepository` at `delivery/sdk/kernel.py:71`, per-process idempotency + approvals → primary D0-5 target; (2) event bus in-memory both tiers (documented non-critical); (3) usage meter per-process (best-effort); (4) RLS covers only actions+ledger tables — policy/approvals/entitlements lack RLS + don't set the tenant GUC (follow-up isolation task); (5) no consent adapter on the operated plane (T-5 legal-gated). No source code changed.
- [2026-06-28] (claude) **Tracker reset to the Deployment-Readiness Program.** Cleared the W0–W8 wave breakdown (retained in git history + BUILD_JOURNAL Log) and replaced it with phases D0–D6 + a tracked non-code workstream, per the decisions of record above. Grounded in a fresh code pass that corrected stale go-live notes (pyproject + durable entitlements + SaaS entrypoint already exist; lockfile + SAML + proof-anchor + push-HITL + MCP/gateway-tool-calls are the real gaps). No code actioned yet.
