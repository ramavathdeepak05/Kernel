# ALIS Backend Dependencies
# Install: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Settings ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.4.0

# --- Database ---
psycopg2-binary==2.9.9
alembic==1.13.2
sqlalchemy==2.0.35          # used by alembic env only

# --- Async Tasks ---
celery[redis]==5.4.0
redis==5.0.8

# --- AI / LLM ---
langgraph==1.1.2
langchain-core>=1.2.18,<2.0.0  # upper bound keeps langgraph 1.x compatible
langchain-ollama==0.3.10
langchain-openai==1.1.11    # OpenAI-compatible APIs (NVIDIA NIM, OpenAI, etc.) — requires openai>=2.x
openai==2.28.0
httpx==0.27.2               # Ollama HTTP client + TestClient async support

# --- File Storage ---
minio==7.2.8

# --- PDF Generation ---
reportlab==4.2.2

# --- Excel Export (M8) ---
openpyxl==3.1.5

# --- Payment ---
razorpay==1.4.1

# --- Auth / Security ---
pyotp==2.9.0                # TOTP / MFA
cryptography==43.0.1        # Fernet encryption for TOTP secrets at rest
PyJWT==2.9.0                # MFA challenge token signing
asyncpg==0.29.0             # async PostgreSQL driver (FastAPI handlers)
hvac==2.3.0                 # HashiCorp Vault client (P0-2)

# --- Observability ---
prometheus-client==0.21.0   # Prometheus metrics exposition
sentry-sdk[fastapi]==2.14.0 # Error tracking + performance monitoring (optional — set SENTRY_DSN to enable)

# --- Utilities ---
python-dateutil==2.9.0
orjson==3.10.7              # fast JSON serializer
asteval==1.0.2              # safe expression evaluator for PolicyEngine DSL (replaces eval())

# --- Auth / Crypto ---
bcrypt==4.2.0

# --- Testing ---
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
fakeredis==2.34.1           # in-memory Redis for unit tests
