apiVersion: v1
kind: Service
metadata:
  name: {{ include "quaicu-kernel.fullname" . }}
  labels:
    {{- include "quaicu-kernel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quaicu-kernel.selectorLabels" . | nindent 4 }}
