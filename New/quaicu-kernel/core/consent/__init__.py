"""K·04 DPDP Consent — core domain package.

This package is hexagonally pure (F-08): no DB drivers, no SDKs, no concrete adapters.
It depends only on `core.ports.consent`, `core.lifecycle.protocols`, `core.types`, and
`core.errors`.
"""
