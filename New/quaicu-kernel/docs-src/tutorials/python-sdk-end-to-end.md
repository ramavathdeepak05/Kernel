# Python SDK End-to-End

!!! info "Coming soon"
    This tutorial is being written. It covers: `Kernel.from_config()` → `@kernel.governed` → `GovernanceProfile` presets → reading an inclusion proof from the ledger.

    See the [SDK Reference →](../reference/sdk/kernel-class.md) for the API surface in the meantime.

**What you'll learn:**

- Installing the kernel Python package
- Writing `kernel.toml` configuration
- Using `@kernel.governed` without changing your function signature
- Reading a sealed ledger entry programmatically
